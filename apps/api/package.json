{
  "name": "tempotrackers-api",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "vercel-build": "python -m pip install --upgrade pip && pip install -r requirements.txt"
  }
}
